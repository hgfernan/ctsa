# Todo list

## 2025-03-06

1. **DONE** *Select characteristics of a processor that module `cpuinfo` lists*;
2. Add them to `run_code.py`;
3. **DONE** *Improve command line parameters of `run_code.py`*;
4. Make `run_code.py` collect the information from the JSON files a test generates;
5. Make `run_code.py` call module `cpuinfo` and add that info to the result file generated by the code execution;
6. Run the codes and collect the data;
7. Generate a template with the 2nd form of AR tests;
8. Run the codes and collect the data of the 2nd form of AR tests;
9. Make `gen_code.py` generate the CMake script, from DeepSeek template;
10. Create ARIMA scripts using `ctsa`;
11. Create ARIMA scripts in Python for `statsmodels` equivalent to the `ctsa` ARIMA tests;
12. Create ARIMA scripts in Python for `forecast` equivalent to the `ctsa` ARIMA tests;
13. Adapt `gen_code.py` to create Python scripts for ARIMA using `statsmodels`;
14. Adapt `gen_code.py` to create R scripts for ARIMA using `forecast`;
15. Adapt `run_code.py` to the new command line parameters;
